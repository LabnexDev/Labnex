{
  "name": "labnex-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @labnex/executor && npm run build -w @labnex/cli && npm run build -w @labnex/cloud-runner"
  },
  "devDependencies": {
    "@types/node": "^20.11.19",
    "@vitejs/plugin-basic-ssl": "^2.0.0",
    "@vitejs/plugin-react": "^4.5.2",
    "ts-node": "^10.9.1",
    "typescript": "^5.4.5",
    "vite-bundle-analyzer": "^0.23.0",
    "vite-plugin-compression": "^0.5.1"
  }
}
