module.exports = {
  preset: 'ts-jest',
  testEnvironment: 'node',
  // Look for test files with `.test.ts` suffix across the project
  testMatch: ['**/*.test.ts'],
  moduleFileExtensions: ['ts', 'js', 'json', 'node'],
}; 